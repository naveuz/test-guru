<h3 class="my-3"><%= t('.header') %></h3>
<p><%= link_to t('.new_badge'), new_admin_badge_path, class: "btn btn-info" %></p>
<%= render 'common/errors', resource: @badge %>
<table class="table table-bordered table-sm">
  <tr>
    <th><%= t('.title') %></th>
    <th><%= t('.image') %></th>
    <th><%= t('.rule') %></th>
    <th><%= t('.rule_option') %></th>
    <th><%= t('.actions') %></th>
  </tr>
  <%= render @badges %>
</table>
<%= link_to t('.back'), admin_root_path %>
